# Settings
windowTitle           = "$TM_DISPLAYNAME - $TM_DIRECTORY (git: $TM_SCM_BRANCH)"

# Extra files to include
#
myIncludes = ".tm_properties,.htaccess,.gitignore,.standalone_migrations,.env,.foreman,.powenv,.travis.yml,.bowerrc,.nvmrc,.rspec,.buildpacks"
fileBrowserGlob  = "{*,$myIncludes}"
include = "{$include,$myIncludes}"

# Hide log and tmp directories from search popups.
#
myExcludes = "log,tmp,public/media,node_modules"
excludeInFileChooser  = "{$excludeInFileChooser,$myExcludes}"
excludeInFolderSearch = "{$excludeInFolderSearch,$myExcludes}"

# Default editing configuration
#
softWrap         = false
showInvisibles   = true

# File type specific configuration
#
[ *.{icns,ico,jpg,jpeg,m4v,nib,pdf,png,psd,pyc,rtf,tif,tiff,xib,gem,mp4,mov} ]
binary           = true

[ source.php ]
softTabs         = false
tabSize          = 4

[ source.python ]
softTabs         = false
tabSize          = 4

[ source.json ]
tabSize          = 2

[ attr.untitled ]
fileType         = "source.php"

[ text.plain ]
softWrap         = true

[ "{README,INSTALL,LICENSE,TODO}" ]
fileType         = "text.plain"
